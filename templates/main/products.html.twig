{% extends 'base.html.twig' %}

{% block title %}Products - Our Store{% endblock %}

{% block body %}
    <div class="container my-5">
        <h1 class="mb-4 fw-bold">Our Products</h1>
        
        <div class="row row-cols-1 row-cols-md-3 g-4">
            {% for product in products %}
                <div class="col">
                    <div class="card h-100 shadow-sm border-0 bg-light-subtle transition hover-shadow">
                        {% if product.image %}
                            <img src="{{ asset('images/' ~ product.image) }}" class="card-img-top p-3" alt="{{ product.name }}" style="max-height: 220px; object-fit: contain;">
                        {% else %}
                            <img src="{{ asset('images/item.png') }}" class="card-img-top p-3" alt="{{ product.name }}" style="max-height: 220px; object-fit: contain;">
                        {% endif %}
                        <div class="card-body d-flex flex-column">
                            <div class="mb-2">
                                <span class="badge bg-primary">{{ product.category.name }}</span>
                                {% if product.isTop %}
                                    <span class="badge bg-warning text-dark">TOP</span>
                                {% endif %}
                            </div>
                            <h5 class="card-title fw-semibold">{{ product.name }}</h5>
                            <p class="card-text text-muted flex-grow-1" style="display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical; overflow: hidden;">
                                {{ product.description }}
                            </p>
                            <p class="card-text fw-bold text-success fs-5">${{ product.price|number_format(2) }}</p>
                            <a href="{{ path('app_product_details', { id: product.id }) }}" class="btn btn-outline-primary mt-auto">View Details</a>
                        </div>
                    </div>
                </div>
            {% else %}
                <div class="col-12 text-center py-5">
                    <p class="text-muted">No products found.</p>
                </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}
